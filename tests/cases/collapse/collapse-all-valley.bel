paper square

mark --ac = map .b onto .d     ; diagonal a–c (folds b onto d)
mark --bd = map .a onto .c     ; diagonal b–d

flatten --ac & .a and --ac & .c and --bd & .b and --bd & .d

; expect error "Maekawa"
