; status: errors — a `standing` clause asks to keep a flap upright (a non-flat
; end state); the collapse kernel only produces flat states so far.

paper square

mark --ac = map .b onto .d     ; diagonal a–c
mark --bd = map .a onto .c     ; diagonal b–d

collapse --ac & .a and --ac & .c and --bd & .b and --bd & .d
  and standing .a

; expect error "standing"
