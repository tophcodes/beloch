; status: works — precrease a diagonal, flip the paper, then reuse the crease:
; material tracking means --q resolves to the FLIPPED diagonal, not the original (#28)
paper square
mark --q = through .a .c
flip
mark perp --q through .b

; assert faces = 1
