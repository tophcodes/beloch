paper square

mark --diag = map .a onto .c
mark --ray = through .a .c

step left
mark --l1 = map --ab onto --diag
mark --l2 = map --da onto --diag
flatten (--l1) (--l2) (--ray & .a) (--ray & .c) {toward .d}

step right
mark --l3 = map --cd onto --diag
mark --l4 = map --bc onto --diag
; BLOCKED ON #48: after ear 1 commits the classic spine, --l4's mark reflects
; across the folded joint and cannot re-materialize on the far flap ("--l4 has
; no material segment"; see tests/cases/collapse/flatten-two-ears-sequential.bel).
; Uncomment the second ear when the kernel rewrite lands:
; flatten (--l3) (--l4) (--ray & .c) (--ray & .a) {toward .d}
