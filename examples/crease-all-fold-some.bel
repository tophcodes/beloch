; status: works — the bare bind creases BOTH layers flat; fold with `up to`
; folds only the top-right flap, the bottom layer keeps its flat F mark
paper square
fold map .d onto .a
mark --m = map .c onto .d
fold --m moving .c up to .c
