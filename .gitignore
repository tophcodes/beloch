scratch/

# /teach learning workspace (lessons, records, mission — local only)
learn/

# Full-text sources (copyrighted books/papers) — metadata lives in paper/references.bib
refs/

# OCaml / dune
_build/
*.install
.merlin

# nix / direnv
.direnv/
result
result-*

# Node tooling — bun workspaces under packages/ — deps reproduced via bun install
node_modules/

# menhir conflict report (generated by --explain)
packages/core/lib/parser.conflicts

# fold2svg / beloch render scratch in the repo root
/*.fold
/*.svg
/*.png

# Snapshot directories for notation-cutover slices
packages/core/tests/golden/_pre_slice1/
