paper square

mark (map .b onto .d) as --ac  ; diagonal a–c
mark (map .a onto .b) as --v   ; vertical midline x = 1/2

flatten (--ac & .a) (--ac & .c)
  (--v & --cd) (--v & --ab)

; expect error "flat-foldable"
