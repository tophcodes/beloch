; Axiom 7 folding .a onto --cd and .c onto --da: the cubic has two common
; tangents, and only one of them cuts the sheet. The paper-incidence filter
; drops the other, so the fold resolves without `toward`.

paper square
fold map .a onto --cd and .c onto --da

; assert steps = 1
; assert faces = 2
