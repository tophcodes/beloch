; AI-generated: this file was written by Claude, an AI model.

paper square

mark (through .a .b) as --b
mark (map .b onto .a) as --v
.mid = --b * --v
fold (map .mid onto .d) (moving .mid)

; assert faces = 2
; assert .mid paper = (1/2, 0)
; assert --b is boundary
