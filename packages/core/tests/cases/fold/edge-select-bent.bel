; AI-generated: this file was written by Claude, an AI model.

; A fold through the midpoint of --ab bends it (edge-bent.bel); a --[…]
; selector still names one of its pieces, whose table line is well defined.
paper square
.m = free on --ab from .a at 1/2
fold (through .c .m) (moving .b)
mark (map --[.a .m] onto --cd) as --x
.q = free on --da from .a at 1/2

; assert .q incident --x
