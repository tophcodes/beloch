paper square

def diagonal(.p .q) {
  mark --d = through .p .q
}

$d1 = apply diagonal(.a .c)
$d2 = apply diagonal(.b .d)

export { --d as --d1d } $d1
export { --d as --d2d } $d2
.m = --d1d * --d2d

; assert faces = 1
; assert .m = (1/2, 1/2)
; assert steps = 1
