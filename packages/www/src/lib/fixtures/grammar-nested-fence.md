# Nested fixture

A four-backtick block quoting a `grammar` fence as a sample, so the sample is
not a fence of its own to remark.

````
```grammar
fake := CREASE_NAME
```
````

```grammar
real := CREASE_NAME
```
