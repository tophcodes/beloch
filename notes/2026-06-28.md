# 2026-06-28 — Project restart, scaffolding laid

Picked the project back up after the Apr 18–20 design conversation. That
conversation settled the *method*; today is about turning it into a concrete
starting point.

What got captured today:

- Seven decision records ([0001](../decisions/0001-ocaml-core-typescript-edge.md)–[0007](../decisions/0007-evaluator-not-compiler.md))
  for everything already decided: OCaml core / TS edge, FOLD-extended output,
  restart-from-minimal-core, Menhir-then-Tree-sitter, the name Beloch, MIT
  license, evaluator-not-compiler + CLI shape.
- `bibliography.md` seeded with Hull, Demaine/O'Rourke, Caruana–Pace 2007,
  Hull–Zakharevich 2023, FOLD.
- `antipatterns.md` seeded with the 2018 dead ends (layer-number heuristic,
  region/layer conflation, accumulate-then-formalize, "unique fold").
- `spec/` and `paper/` README stubs with the planned shape.
- OCaml/dune/menhir project skeleton + a Nix flake devshell (`nix develop` /
  `direnv allow`).

## Where the real work starts next

The minimal core (v0.0) is the first actual *thinking*, deliberately tiny:
one square, **axiom 1 only** (`fold .a to .b` → a crease line through two
points), one type (crease line), output = list of creases as FOLD-extended. No
layers, no folded state, no regions, no composition. Goal: `beloch fold` runs
end-to-end on a one-line program.

Open question to chew on before writing `spec/10-core-v0.md`: what *exactly* is
the geometric state the evaluator threads through? For axiom 1 alone it's just
"the paper polygon + a set of crease segments" — but pinning the type now, even
minimally, is the design.
